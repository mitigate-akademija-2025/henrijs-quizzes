<li>
  <% if current_page.first? %>
    <span class="inline-flex items-center rounded-lg border border-gray-100 bg-gray-50 px-3 py-1.5 text-sm text-gray-400 cursor-not-allowed">
      «&nbsp;First
    </span>
  <% else %>
    <%= link_to raw("«&nbsp;First"), url,
      class: "inline-flex items-center rounded-lg border border-gray-200 bg-white px-3 py-1.5 text-sm text-gray-700 shadow-sm hover:bg-gray-50",
      data: { turbo_frame: "completed_quizzes" } %>
  <% end %>
</li>
